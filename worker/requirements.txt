# Uprising Clip – Worker (transcription, LLM, rendu)
supabase>=2.0.0
python-dotenv>=1.0.0
# Transcription (optionnel : décommenter pour Whisper local)
# openai-whisper>=20231117
# ffmpeg-python>=0.2.0
# Appels LLM (optionnel)
# google-generativeai>=0.3.0
# httpx pour requêtes HTTP
httpx>=0.25.0
